{
	"scripts": {
		"build:client": "cd client && npm run build && cp -R ./build ../public && cp -R ./node_modules ../public/node_modules",
		"build:server": "cd server && npm run build && cp -R ./dist ../dist && cp -R ./node_modules ../dist/node_modules",
		"build": "concurrently \"npm run build:server\" \"npm run build:client\"",
		"start": "npm run build && node ./dist/main.js",
		"postinstall": "concurrently \"cd client; npm i\" \"cd server; npm i\""
	},
	"dependencies": {
		"concurrently": "^6.2.0"
	}
}
